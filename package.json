{
  "name": "mercuril-site",
  "version": "0.1.0",
  "private": true,
  "description": "MercuriL Systems — Totem flood-detection landing site",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "mkdir -p public/vendor && cp node_modules/maplibre-gl/dist/maplibre-gl.js node_modules/maplibre-gl/dist/maplibre-gl.css public/vendor/"
  },
  "dependencies": {
    "@turf/turf": "^7.3.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "maplibre-gl": "^5.24.0",
    "pg": "^8.22.0"
  }
}
