PORT=3000

# Neon Postgres connection string (Neon dashboard → Connection Details →
# "Pooled connection"). Keep secret — never commit the real value.
# Format: postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=
