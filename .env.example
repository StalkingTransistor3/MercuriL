PORT=3000

# Neon Postgres — closures (real gov data), sensors, readings, inquiries, ETL log.
# Neon dashboard → Connection Details → "Pooled connection". Keep secret —
# never commit the real value; it lives only in .env (gitignored) and Railway Variables.
DATABASE_URL=

# Gate for /admin, sensor CRUD and manual ETL refresh
ADMIN_KEY=
